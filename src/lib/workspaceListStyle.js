/**
 * Shared list/table chrome for workspace pages (aligns with Sales dense lists).
 * Use with a 12-column grid inside each row header cell.
 */
export const WORKSPACE_TABLE_HEAD =
  'hidden sm:grid grid-cols-12 px-4 text-[9px] font-semibold text-slate-400 uppercase tracking-wider border-b border-slate-200 pb-2 mb-2 gap-1';

/** Empty list block — same rhythm as Sales quotation search miss */
export const WORKSPACE_EMPTY_LIST_CLASS =
  'rounded-xl border border-dashed border-slate-200 bg-slate-50/50 py-14 px-6 text-center';
